# 项目当前状态（2026-03-25）

## 已完成
- 工程可编译可启动（`cmake --build build --config Debug` 通过）
- 主界面单页框架与中文文案完成
- 配置与日志模块完成（`ConfigManager` / `LogService`）
- 板卡模拟接入完成（连接、DI轮询、状态显示）
- 运动控制模拟链路完成（点动、绝对/相对、连续旋转、停止/急停）
- 光机串口控制完成（总开关、LED开关、亮度百分比）
- 图片目录解析完成（`*deg` 角度提取，支持前后缀与非等步长序列）
- 旋转与投影同步流程完成（有限循环/无限循环）
- 新增投影窗口：同步时自动显示并切换图片（优先副屏全屏）

## 仍为模拟/占位
- 粗零点与精零点流程目前是模拟分支
- 相机功能区目前为占位UI，尚未接入 `CameraService`
- 板卡尚未接入真实 `COM_GAS_N` DLL 调用

## 本地运行
1. `cmake -S . -B build`
2. `cmake --build build --config Debug`
3. 运行 `build/Debug/RotaryTableControl.exe`

## 下一步建议
1. 接入真实板卡 DLL（M2/M3实机化）
2. 实现粗/精回零状态机与联锁（M4/M5）
3. 接入相机枚举/预览/拍照/录像（M6）
4. 完善异常联锁矩阵并做联调验收（M7）
